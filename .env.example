# CANDIY API 키 (필수) — x-api-key 헤더로 전송
VITE_API_KEY=your_candiy_api_key

# (선택) API 베이스 URL.
# 미설정 시 dev 프록시 경로 '/candiy/v1' 사용 → vite.config.ts의 proxy가
# https://api.candiy.io 로 포워딩하여 브라우저 CORS를 회피한다.
# 직접 호출하려면 아래 주석을 해제 (CANDIY가 CORS 허용해야 함):
# VITE_API_BASE_URL=https://api.candiy.io/v1
